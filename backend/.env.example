# Server
PORT=3001

# GPU worker HTTP endpoint (local dev or RunPod endpoint URL)
GPU_WORKER_URL=http://localhost:8080

# Public URL the worker uses to call back and fetch uploads
BACKEND_PUBLIC_URL=http://localhost:3001

# Optional: use mock worker responses when GPU worker is unavailable
USE_MOCK_WORKER=false
